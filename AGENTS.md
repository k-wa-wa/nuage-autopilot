# AGENTS.md

このリポジトリで作業するエージェント向けの規約である。人間向けの概要は [README.md](./README.md)、
設計判断とその理由は [DESIGN.md](./DESIGN.md) にある。**着手前に DESIGN.md を読むこと。**

## 検証コマンド

変更を終える前に必ず次を通すこと。

```sh
go build ./...
go vet ./...
go test ./...

nix build .#nuage-autopilot
```

## コードの規約

- コメント・ドキュメントは日本語、**である調**で書く。
- doc コメントには「何をするか」ではなく **「なぜそうしたか」** を書く。既存コードがこの水準で
  書かれているため、同じ密度に合わせること。
- 設計上の判断に対応するコメントには **`DESIGN.md N章` / `DESIGN.md N.M 節` の形で参照を書く**。
  節番号を書くときは、実際にその節が存在し内容が一致することを確認すること。
- エラーはパッケージ名を接頭辞に付けて包む（例: `fmt.Errorf("store: acquire lease for item %d: %w", id, err)`）。
- テスト関数には、それがどの設計上の性質を守っているのかを 1〜2 行の日本語コメントで書く。
- 時刻を SQLite に書く際は `store` パッケージの `formatTime` を使う。列の文字列比較が
  そのまま時系列比較として成立する固定長書式に揃えているため、生の RFC3339 を書き込まない。

## 変更時に守ること

- **DESIGN.md は仕様である。** 挙動・遷移・環境変数・ディレクトリ構成を変えたら、DESIGN.md の
  該当節も同じコミットで更新する。コード側の節参照も追随させる。
- DB スキーマの変更は `internal/store/migrations/` に**新しい番号のファイルを追加**して行う。
  適用済みのマイグレーションファイルを書き換えてはならない。

## 触れてはならないもの

- **シークレットをログ・標準出力・コミットに出さないこと。** `GH_TOKEN` の値、エージェントに
  渡すプロンプト全文はログに流さない設計になっている（`internal/runner` が claude の出力だけを
  ログに流すのはこのためである）。
- `vendor/` 配下のコードを手で編集しないこと。

## 用語

| 語 | 意味 |
| :-- | :-- |
| アイテム (item) | 追跡対象の 1 つの Issue または PR |
| phase | アイテムがストーリー上どこまで進んだかを表す永続状態 |
| lease | 「今この瞬間そのアイテムを処理中か」を表す TTL 付きの排他制御 |
| 人間チャネル | GitHub のコメント・PR 本文。エージェントが自由に書く |
| 機械チャネル | `NUAGE_REPORT_FILE`。outcome とサブ Issue 番号のみを載せる |
| 着火 | イベントを受けてエージェントを起動すること |
